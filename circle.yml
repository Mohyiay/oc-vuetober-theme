machine:
  environment:
    PATH: "${PATH}:${HOME}/${CIRCLE_PROJECT_REPONAME}/node_modules/.bin"
  node:
    version: 8.9.4

dependencies:
  override:
    - yarn
  cache_directories:
    - ~/.cache/yarn

test:
  pre:
    - npm rebuild node-sass --force
  override:
    - yarn run unit-once
  post:
    - bash <(curl -s https://codecov.io/bash) -t 64f90935-8f54-4334-b44e-acc26be0c125
